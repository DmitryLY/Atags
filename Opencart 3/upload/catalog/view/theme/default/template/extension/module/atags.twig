<style>

    .atags {
        padding: 10px;
    }

    .atags a {
        padding: 10px;
        margin: 5px;
        display: inline-block;
        border: 1px solid #efefef;
        box-shadow: 0px 0px 5px #ededed;
        transition: box-shadow 0.1s ease-in-out;
    }

    .atags a:hover {
        box-shadow: 4px 4px 5px #ededed;
    }

</style>

<div class="atags">
    {% for atag in atags %}
        <a href="{{atag.href}}">{{atag.short_name}}</a>
    {% endfor %}
</div>